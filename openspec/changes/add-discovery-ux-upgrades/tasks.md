## 1. Implementation
- [x] 1.1 Make web resume launch the stored agent instead of always launching Claude
- [x] 1.2 Add a web search API and UI flow
- [x] 1.3 Extend `shiftlog list` with filters and JSON output
- [x] 1.4 Extend `shiftlog search` with JSON output, highlighted hits, and direct show handoff
- [x] 1.5 Fix branch conversation counts for long-lived branches and add a load-more history control
- [ ] 1.6 Run targeted tests and fix regressions
